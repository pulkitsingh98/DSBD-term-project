# Freight Damage Claims Analytics - interactive presentation
#
#   pip install -r requirements.txt
#   python train_models.py
#   python app.py            ->  http://127.0.0.1:5000
#
# Versions below are the minimums this project was developed and verified
# against (Python 3.11).

Flask>=3.0
pandas>=2.0
numpy>=1.26
scikit-learn>=1.4
scipy>=1.11
joblib>=1.3
openpyxl>=3.1          # reads the data dictionary that drives the leakage split
plotly>=5.18           # only for its bundled plotly.min.js, served locally
gunicorn>=21.2
